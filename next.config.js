// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['drive.google.com', 'www.sikhiwiki.org'], // Add any other domains you'll load images from
  },
};

module.exports = nextConfig;
