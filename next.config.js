// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['google.com', 'sikhiwiki.org'], // Add any other domains you'll load images from
  },
};

module.exports = nextConfig;
